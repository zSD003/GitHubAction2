{
  "name": "devops",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "build": "set NODE_OPTIONS=--opensl-legacy-provider && webpack --config config.js src/app.js -o public/bundle.js",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider",
    "deploy": "firebase deploy --token \"$FIREBASE_TOKEN\" --only hosting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^14.2.0",
    "jest": "^25.1.0",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "terser": "^4.6.6"
  }
}
